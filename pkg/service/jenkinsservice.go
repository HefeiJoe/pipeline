package service

import (
	//"bytes"
	//"net/http"
)

func Build(job string, body interface{}) error{
	//client := &http.Client{}
	//req, err := http.NewRequest("POST", "http://127.0.0.1", bytes.NewReader(postData))
	//if err != nil{
	//
	//}
	//req.Header.Add("User-Agent", "无敌浏览器")
	//resp, err := client.Do(req)
	//defer resp.Body.Close()
	//return resp, err
}
